home:
  path:     /
  defaults: { _controller: AppBundle:Default:index}

registration:
  path: /register
  defaults: {_controller: AppBundle:Auth:registration}

login:
  path: /login
  defaults: {_controller: AppBundle:Auth:login}

logout:
  path: /logout

add_team:
  path: /addTeam
  defaults: {_controller: AppBundle:Admin:addTeam}

add_free_days:
  path: /addFreeDays
  defaults: {_controller: AppBundle:Admin:addFreeDays}

account:
  path: /account
  defaults: {_controller: AppBundle:User:myAccount}

send_email:
  path: /sendEmail
  defaults: {_controller: AppBundle:Admin:index}

delete_day_off:
  path: /deleteDayOff/{id}
  defaults: {_controller: AppBundle:User:deleteDayOff}

delete_free_day:
  path: /deleteFreeDay/{id}
  defaults: {_controller: AppBundle:Admin:deleteFreeDay}

users:
  path: /users
  defaults: {_controller: AppBundle:Admin:getUsers}

number_of_days_off:
  path: /numberOfDaysOff
  defaults: {_controller: AppBundle:Admin:addNumberOfFreeDayForUser}
